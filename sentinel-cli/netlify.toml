[build]
  base = "sentinel-cli"
  command = "npm run build"
  publish = ".next" # or "out" if you're using next export
  functions = "netlify/functions"

  [build.environment]
    NODE_ENV = "production"
    FIREBASE_PROJECT = "trs-operator-live"
